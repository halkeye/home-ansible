- name: Dozzle
  community.docker.docker_container:
    name: dozzle
    image: amir20/dozzle
    state: started
    auto_remove: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    labels:
      traefik.enable: "true"

